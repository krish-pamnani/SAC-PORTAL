# Backend Environment Variables
# Copy this to .env and fill in your actual values

# Server Configuration
PORT=5000
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here

# JWT Secret (use a cryptographically random value)
# Generate using: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Encryption Key for Bank Account Numbers (32 bytes = 64 hex chars)
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BANK_ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# Email Configuration (Resend API - https://resend.com)
RESEND_API_KEY=your-resend-api-key
EMAIL_FROM=IPM SAC Treasury <onboarding@resend.dev>

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000

# Allowed Email Domains
ALLOWED_EMAIL_DOMAIN=iimidr.ac.in

# Admin Setup Key (required for /api/auth/setup/* routes)
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SETUP_KEY=your-admin-setup-key-here
